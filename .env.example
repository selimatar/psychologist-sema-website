VITE_API_BASE_URL="http://localhost:4000"

# Same Sanity project used by sanity/.env's SANITY_STUDIO_PROJECT_ID — this
# is just the public, read-only content client (no token, useCdn: true).
VITE_SANITY_PROJECT_ID="replace-with-project-id"
VITE_SANITY_DATASET="production"
VITE_SANITY_API_VERSION="2024-01-01"
